# v1.3.0

发布日期：2026-08-31。

Windows 用户请下载 `WeChat-TXT-PDF-Exporter-Installer-v1.3.0.exe`。

- 新增软件内版本与更新窗口：静默后台检查、24 小时自动检查限频、可关闭且按版本去重的提醒、版本历史、内部下载和 SHA256 校验。GitHub 不可达时仅显示状态，不影响离线导出。
- 安装版与便携版使用独立更新进程，等待原程序退出后安装/替换并重启。下载失败保留当前版本；替换或启动失败尝试恢复旧程序。
- 修复多账号及重启后切换账号的连接错误：当前进程账号优先，登录前不锁定猜测目录，账号不明确时安全停止。
- 优先尝试无重启连接，仅在确实需要启动捕获时由用户确认关闭/启动微信；软件启动不再主动弹连接确认。
- 缓存非敏感账号提示、微信安装位置及 codec 定位信息；DLL 路径、大小或修改时间变化时失效。密钥和登录凭据不落盘。
- 安装目录中的程序改用稳定名称 `WeChat-TXT-PDF-Exporter.exe`，覆盖安装清理旧版本 EXE；下载附件仍带版本号。构建默认不覆盖开发者已安装版本。
- 加强连接失败和正常退出的临时解密数据库清理；关闭时等待正在工作的线程结束。
- 保留现有 TXT/PDF、朋友圈 HTML/JSON/媒体归档、历史记录功能。按需解密完成架构评估，本版本暂不启用。

建议在该微信账号长期使用的 Windows 电脑上运行。本工具仅读取这台电脑本地已经保存的微信数据库，不会从手机或微信云端补齐完整历史记录。

聊天数据库仅在本机处理，密钥只保存在内存，不上传聊天数据，不修改微信；检查更新及部分媒体获取功能可能联网。无需保存微信 Cookie 或其他登录凭据。

v1.2 用户首次需手动运行 v1.3 安装包；以后才可使用软件内更新。未配置商业代码签名；SHA256 校验不能替代可信来源或数字签名。

验证范围：122 项本地自动测试、离线打包自检及合成 EXE 的独立更新进程实测通过。真实微信登录/账号切换与真实 Inno 跨版本升级尚未完成人工回归，建议先以少量会话验证后再批量导出。已知边界与人工测试清单详见 [v1.3 开发说明](https://github.com/julibeian/wechat-txt-pdf-exporter/blob/v1.3.0/docs/v1.3.0-development.md)。

# v1.2.0

Windows 用户请下载 `WeChat-TXT-PDF-Exporter-Installer-v1.2.0.exe`。

数据来源：聊天和朋友圈记录读取本机微信数据库，不会从云端拉取全部历史。朋友圈图片、视频可根据本机记录中的链接联网下载，也会读取本机缓存；导出内容在本机处理和保存，不上传。

本次更新（相较已发布的 v1.0.0）：

- 朋友圈支持导出离线 HTML + JSON + 原始照片/视频 + SHA-256 清单；导出后可断网阅读。选择“我自己”可保存本机已有的本人朋友圈，包含私密和分组可见动态，并标注可见范围。
- 完善普通视频、实况照片和动画归档，修复媒体解析与解密问题；实况照片动态部分不可用时优先显示自带静态主图，并明确标注，不再堆积大块错误提示。缩略图和缺失媒体也会如实标记。
- Star 提示保留在软件内部，移至导出区域底部；关闭后释放的空间用于增高联系人/群聊选择列表。
- 联系人和群聊按 A–Z/拼音首字母排序并用字母分组行分隔；聊天与朋友圈分别标注预计耗时，导出历史窗口同步放大。
- 聊天 TXT/PDF 补全回复引用原文和合并转发的逐条记录。
- 修复 Windows 归档目录改名时的 `WinError 5` 拒绝访问问题，增加重试与完整目录复制兜底。
- 主界面与使用说明明确区分本机记录、联网下载媒体和不上传内容。

适用 Windows 10/11 x64、微信 4.x（已针对 4.1.12.55 验证）。本地 58 项测试及打包自检通过。无须安装 Python；安装包尚未配置商业代码签名证书。

# v1.1.0

新增“导出该人朋友圈 PDF”：

- 仅在勾选 PDF 且单选联系人时可用；群聊、多选或 TXT-only 状态不可用。
- 读取微信 4.x 本机 `sns.db / SnsTimeLine` 数据，导出该联系人本机已同步且当前可见的全部朋友圈。
- 置顶内容单列，其余按日期归类。
- 照片优先使用微信官方 CDN 原图，不做额外有损重编码；PDF 预览可点击跳转到原图页放大。
- 原图不可用时明确标注缩略图兜底或缺失，不静默降低质量。
- 输出至“联系人/联系人名称/朋友圈”目录，仅生成 PDF。

# v1.0.0

首个公开正式版。

- 联系人、群聊和每个会话分别保存。
- 支持 TXT、快速 PDF 和带图片/表情的完整 PDF。
- “类型 ▾”表头可折叠筛选全部类型、联系人或群聊。
- 快速模式默认启用，完整模式可手动选择。
- TXT/PDF 默认写入微信已有语音转文字，并提供图文操作说明。
- 放大导出历史窗口，可直接打开文件位置。
- 连接微信 60 秒后以小型滑入窗口提示点亮 GitHub Star；本次关闭后不再显示。
- 提供 Windows 10/11 x64 一键安装包。

Windows 用户请下载 `WeChat-TXT-PDF-Exporter-Installer-v1.0.0.exe`。
