# v1.2.0

朋友圈导出升级为适合大量照片和视频的离线归档：

- 聊天 TXT/PDF 现在会导出回复消息引用的原文，并展开合并转发聊天记录中的逐条内容。
- 使用静态 HTML 时间线阅读，置顶内容单列，其余按日期归类；无需安装服务或联网。
- 照片、普通视频和实况照片视频保存到独立 `media/` 目录；照片点击可打开原文件，视频可直接播放。
- 同步生成 UTF-8 `moments.json` 机器可读索引和 `manifest-sha256.txt` 完整性清单。
- 修正 ISAAC-64 初始化常量错误，已与微信官方 WASM 固定向量逐字节校验，并验证 128 KiB 视频前缀解密。
- 修复朋友圈 XML 中嵌套 `livePhoto` / `liveMedia` 视频未被解析的问题。
- 朋友圈归档不再要求勾选聊天 PDF；仍仅支持单选联系人，不支持群聊。
- 联系人列表新增“我自己”入口，可导出当前登录账号本机保存的全部朋友圈，包括仅自己可见、部分可见和不给谁看等私密动态，并逐条标注可见范围。
- 保留缺失媒体的显式标记，不把缩略图静默冒充原图。
- 实况照片动态媒体不可用时，HTML 优先显示同一动态自带的静态主图，不再铺满错误卡片；独立动画另存最后停止帧作为浏览器兜底。
- 联系人和群聊按 A–Z/拼音首字母分组显示，聊天与朋友圈分别标注预计耗时。
- GitHub Star 提示改为主窗口内的紧凑横幅，导出历史页面同步放大。
- Star 横幅移至导出区域底部；关闭后不保留空白，联系人/群聊选择列表自动增高。
- 修复 Windows 索引器或安全软件短暂占用新文件时，朋友圈归档在最终目录改名阶段报 `WinError 5` 的问题；现在会自动重试，并在持续占用时复制完整归档作为兜底。

# v1.1.0

新增“导出该人朋友圈 PDF”：

- 仅在勾选 PDF 且单选联系人时可用；群聊、多选或 TXT-only 状态不可用。
- 读取微信 4.x 本机 `sns.db / SnsTimeLine` 数据，导出该联系人本机已同步且当前可见的全部朋友圈。
- 置顶内容单列，其余按日期归类。
- 照片优先使用微信官方 CDN 原图，不做额外有损重编码；PDF 预览可点击跳转到原图页放大。
- 原图不可用时明确标注缩略图兜底或缺失，不静默降低质量。
- 输出至“联系人/联系人名称/朋友圈”目录，仅生成 PDF。

# v1.0.0

首个公开正式版。

- 联系人、群聊和每个会话分别保存。
- 支持 TXT、快速 PDF 和带图片/表情的完整 PDF。
- “类型 ▾”表头可折叠筛选全部类型、联系人或群聊。
- 快速模式默认启用，完整模式可手动选择。
- TXT/PDF 默认写入微信已有语音转文字，并提供图文操作说明。
- 放大导出历史窗口，可直接打开文件位置。
- 连接微信 60 秒后以小型滑入窗口提示点亮 GitHub Star；本次关闭后不再显示。
- 提供 Windows 10/11 x64 一键安装包。

Windows 用户请下载 `WeChat-TXT-PDF-Exporter-Installer-v1.0.0.exe`。
